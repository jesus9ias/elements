/** App-level constants. */

/** Brand name — language-agnostic, used for the document title. */
export const APP_NAME = 'Elements';

/** Language the server-rendered document declares before client hydration. */
export { DEFAULT_LANGUAGE as DOCUMENT_LANGUAGE } from './i18n';
