# frontend/.env — copy to frontend/.env and populate. NEVER commit the real .env.
#
# Local-only tooling (content pipeline). NOT read by the app runtime or the
# CI/CD build — only by scripts run on-demand by the developer (see spec.md).
ANTHROPIC_API_KEY=

# Infra-related variables consumed at deploy time live in infra/.env, not here.
